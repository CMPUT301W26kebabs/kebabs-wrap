<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventManager"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".ui.SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fahad's Sign Up is reached after splash for new users -->
        <activity android:name=".ui.EntrantSignUpActivity" android:exported="false" />

        <activity android:name=".ui.ProfileActivity" android:exported="false" />

        <!-- Fahad's onboarding -->
        <activity android:name=".ui.BookedEventsActivity" android:exported="false" />
        <activity android:name=".ui.QrAccessActivity" android:exported="false" />

        <!-- Admin Dashboard (Ibrahim) -->
        <activity android:name=".ui.HomeActivity" android:exported="false" />
        <activity android:name=".ui.MainActivity" android:exported="false" />

        <!-- Ammaar's activities -->
        <activity android:name=".ui.NotificationsActivity" android:exported="false" />
        <activity android:name=".ui.AcceptDeclineActivity" android:exported="false" />

        <!-- Huzaifa's activities -->
        <activity android:name=".ui.BrowseEventsActivity" android:exported="false" />
        <activity android:name=".ui.EventDetailsActivity" android:exported="false" />
        <activity android:name=".ui.InviteGuestsActivity" android:exported="false" />
        <activity android:name=".ui.QRScannerActivity" android:exported="false" />
        <activity android:name=".ui.CreateEventActivity" android:exported="false" />
        <activity android:name=".ui.ManageEventActivity" android:exported="false" />
        <activity android:name=".ui.EntrantMapActivity" android:exported="false" />
        <activity android:name=".ui.ModerateCommentsActivity" android:exported="false" />
        <activity android:name=".ui.ManagePosterActivity" android:exported="false" />
        <activity android:name=".ui.EventQRActivity" android:exported="false" />
        <activity android:name=".ui.MyEventsActivity" android:exported="false" />

        <!-- Follow System -->
        <activity android:name=".ui.OrganizerProfileActivity" android:exported="false" />
        <activity android:name=".ui.FollowerBroadcastActivity" android:exported="false" />

        <!-- Umar's activities -->
        <activity android:name=".ui.EnrolledListActivity" android:exported="false" />
        <activity android:name=".ui.RunLotteryActivity" android:exported="false" />

        <!-- Anas's activities -->
        <activity android:name=".ui.EditProfileActivity" android:exported="false" android:screenOrientation="portrait"/>
        <activity android:name=".ui.AvatarPickerActivity" android:exported="false" android:screenOrientation="portrait"/>
        <activity android:name=".admin.AdminNotificationLogsActivity" android:exported="false" android:theme="@style/Theme.EventManager" />

        <!-- Ibrahim's admin screens -->
        <activity android:name=".admin.AdminEventsActivity" android:exported="false" android:theme="@style/Theme.EventManager" />
        <activity android:name=".admin.AdminImagesActivity" android:exported="false" android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".admin.AdminProfilesActivity" android:exported="false" android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".admin.AdminCommentsActivity" android:exported="false" android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

    </application>
</manifest>
