<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="40dp"
        android:text="Event Manager"
        android:textSize="24sp"
        android:textStyle="bold" />

    <!-- US1: Join Waiting List -->
    <Button
        android:id="@+id/btnGoToEventActivity"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginBottom="16dp"
        android:text="Join Waiting List (US1)" />

    <!-- US2 + US3: Organizer Event Lists -->
    <Button
        android:id="@+id/btnGoToEventLists"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginBottom="16dp"
        android:text="View Waiting / Chosen Lists (US2 &amp; US3)" />

    <!-- US4: Accept Invitation -->
    <Button
        android:id="@+id/btnGoToInvitation"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="Accept Invitation (US4)" />

</LinearLayout>