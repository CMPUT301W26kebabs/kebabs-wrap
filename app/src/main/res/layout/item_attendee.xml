<?xml version="1.0" encoding="utf-8"?>
<!-- item_attendee.xml — used by RecyclerView in activity_manage_event.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="2dp">

    <!-- Section Header (e.g. "A", "B") — GONE when not the first item of a letter group -->
    <TextView
        android:id="@+id/tvSectionHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:layout_marginBottom="8dp"
        android:layout_marginStart="4dp"
        android:fontFamily="@font/poppins_bold"
        android:textColor="#8E6FA7"
        android:textSize="15sp"
        android:textStyle="bold"
        android:visibility="gone"
        tools:text="A" />

    <!-- Attendee Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="0dp"
        android:paddingEnd="0dp">

        <!-- Avatar -->
        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/ivAvatar"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@drawable/ic_person_placeholder"
            app:civ_border_color="#EEEEEE"
            app:civ_border_width="1dp"
            android:contentDescription="Attendee avatar" />

        <!-- Name -->
        <TextView
            android:id="@+id/tvAttendeeName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:fontFamily="@font/poppins_regular"
            android:textColor="#4A365F"
            android:textSize="16sp"
            tools:text="Alex Jim" />

        <TextView
            android:id="@+id/tvStatusAction"
            android:layout_width="76dp"
            android:layout_height="36dp"
            android:gravity="center"
            android:background="@drawable/bg_button_primary"
            android:fontFamily="@font/poppins_bold"
            android:text="Send"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            tools:text="Done" />

    </LinearLayout>

    <!-- Thin Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_marginStart="72dp"
        android:background="#EEEEEE" />

</LinearLayout>
